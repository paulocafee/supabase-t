/**
 * LOCALIZAÇÃO: src/types/cadastro.ts
 * PROPÓSITO: Definição de modelos de dados e interfaces TypeScript.
 */

export interface Cadastro {
  id: string;
  nome: string;
  email: string;
  telefone: string;
}
